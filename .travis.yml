sudo: required

dist: precise

language: 
  - python
  - cpp

python: 
  - "3.4"

script:
  - ./unitframe.py -ut
  - ./gate.py -ut
  - ./gate.py

compiler: gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5

env: COMPILER=g++-5

notifications:
  email: true
